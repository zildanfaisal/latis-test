[build]
builder = "NIXPACKS"
buildCommand = """
composer install --no-dev --optimize-autoloader
npm ci --omit=dev
npm run build
"""

[start]
command = "php artisan serve --host 0.0.0.0 --port ${PORT}"

[variables]
APP_ENV = "production"
APP_DEBUG = "false"
